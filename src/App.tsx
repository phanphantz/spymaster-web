export function App() {
    return (
        <div style={{ padding: 24 }}>
            <h1 style={{ fontSize: 'var(--fs-display)', color: 'var(--steel)', margin: 0 }}>
                SPYMASTER
            </h1>
            <p style={{ color: 'var(--cyan)', fontSize: 'var(--fs-detail)' }}>
                web prototype — scaffolding
            </p>
        </div>
    );
}
